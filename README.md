mbentley/jenkins-demo
==================

docker image for jenkins to use as a demo

To pull this image:
`docker pull mbentley/jenkins-demo`

Example usage:
`docker run -d -p 8080 mbentley/jenkins-demo`
